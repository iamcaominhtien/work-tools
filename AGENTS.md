# AI Agent Instructions
 
Use uv to manage python packages, environment, running and building
 
scripts folder: contains all scripts
tests folder: contains all unit test (pytest). each script has it own folder in tests folder, and it manages test code and test data by itself
 
Always format code with ruff (run by uv)
 
each function and class must have simple doc (never empty)